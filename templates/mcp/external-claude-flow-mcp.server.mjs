#!/usr/bin/env node
// Minimal stdio MCP fallback stub (placeholder)
process.stdin.resume();
process.stdin.setEncoding('utf8');
console.log(JSON.stringify({ ok: true, message: 'External Claude Flow MCP fallback stub active' }));

